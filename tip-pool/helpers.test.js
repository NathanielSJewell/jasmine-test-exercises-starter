//test all functions in helpers.js file

describe(`test all functions in helpers.js file`, function() {
	const paymentObject = { tipAmt: 20, billAmt: 75, tipPercent: 15 };
	sumPaymentTotal(paymentObject);
	expect(total).toEqual();
});
